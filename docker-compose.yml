version: '3.6'
services:
  postgres:
    image: "mdillon/postgis:11"
    volumes:
      - ./tmp/db:/var/lib/postgresql/data
    ports:
      - "5432:5432"

  web:
    build: .
    volumes:
      - .:/pyr
    ports:
      - "3000:3000"
    depends_on:
      - postgres
